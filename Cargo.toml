[package]
name = "klover-rf"
version = "0.1.0"
edition = "2021"

[lib]
name = "jvm"
crate-type = ["cdylib"]

[dependencies]
cafebabe = { path = "../cafebabe", features = ["klover"] }
libc = "0.2"
region = "3.0.2"

[build-dependencies]
cc = "1.0"
