[package]
name = "klover"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
bit-set = "0.8.0"
bitflags = "2.8.0"

# cafebabe = { path = "../cafebabe/" }
cafebabe = "0.8.0"

dashmap = "6.1.0"

# mmtk = { path = "../mmtk-core" }
# mmtk = "0.30.0"

nix = "0.29.0"
num = "0.4.3"
phf = { version = "0.11", features = ["macros"] }

[build-dependencies]
